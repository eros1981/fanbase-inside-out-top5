# BigQuery Configuration
BIGQUERY_PROJECT_ID=758470639878
BIGQUERY_DATASET=reporting_db
# Option 1: JSON credentials as environment variable (recommended for Railway)
GOOGLE_APPLICATION_CREDENTIALS={"type": "service_account", "project_id": "your-project", ...}
# Option 2: Path to credentials file (for local development)
# GOOGLE_APPLICATION_CREDENTIALS=/app/service-account-key.json

# Security
HMAC_SECRET_SHARED=your_shared_hmac_secret_here

# Application Settings
NODE_ENV=production
DEFAULT_TIMEZONE=America/New_York
PORT=80
